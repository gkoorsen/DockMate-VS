Metadata-Version: 2.4
Name: docking_platform_gui
Version: 0.1.0
Summary: Redock analysis GUI with adaptive docking pipeline
Author: Gerrit Koorsen
License: Proprietary
Requires-Python: >=3.9
Description-Content-Type: text/markdown
Requires-Dist: numpy
Requires-Dist: pandas
Requires-Dist: openpyxl
Requires-Dist: rdkit
Requires-Dist: loguru
Requires-Dist: biopython
Requires-Dist: pydantic
Requires-Dist: MDAnalysis

# Redock Analysis GUI

Standalone GUI for redocking analysis with single or adaptive docking, ligand filtering, controls/decoys, and pose inspection.

## Quick start

```
python scripts/launch_redock_analysis_gui.py
```

Or install and run:

```
pip install -e .
redock-gui
```

## External tools

The GUI shells out to several binaries. Install what you need for your workflow:

- Smina or Vina (required for docking): set paths in the GUI.
- rDock (optional): set rDock root in the GUI.
- Open Babel (`obabel`): required for ligand/rescoring conversions.
- PDBFixer + OpenMM: required for protein preparation.
- PROPKA and Reduce: optional, used by protein preparation if available.

## Python dependencies

Core dependencies are in `pyproject.toml`. You may prefer conda for RDKit and OpenMM.

## Project layout

- `docking_platform_gui/gui/redock_analysis.py`: main GUI.
- `docking_platform_gui/adaptive_docking.py`: adaptive protocol logic.
- `docking_platform_gui/preparation/`: protein/ligand preparation.
- `docking_platform_gui/docking/`: docking engine wrappers.
- `scripts/launch_redock_analysis_gui.py`: launch script.

## Notes

- Rescoring uses `smina --score_only`.
- Adaptive docking uses the 5-protocol flow implemented in `adaptive_docking.py`.
